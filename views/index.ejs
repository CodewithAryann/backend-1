<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/views/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block uppercase w-full rounded-md outline-none px-5 py-2 bg-zinc-800 mb-2" type="text" placeholder="title" name="title">
                <textarea class="block w-full uppercase rounded-md outline-none px-5 py-2 resize-none bg-zinc-800" placeholder="Write your text detail" name="details"></textarea>
                <input class="button mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create task">
            </form>

        </div>
        <div class="task p-10 flex gap-4 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach (function(val){ %> 
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class="flex w-full items-center justify-between mt-5">
                            <a class="text-blue-300 inline-block" href="/file/<%= val %>">Read more</a>
                            <a class="text-zinc-600" href="/edit/<%= val%>">edit filename</a>
                        </div>
                    </div>
                    <% }) %>
                <% } else { %>
                    <h3 class="text-zinc-600 uppercase ">No task yet</h3>
                <% } %>
        </div>
    </div>
</body>
</html>